
-- create vertual enviornment --
        1. python3 -m venv venv

-- Active enviornment--
        1. source venv/bin/activate

-- update requirmrnts.txt file--

        1. pip freeze > requirements.txt

-- AWS loggins command --

        1. chmod 400 "awsPemKey.pem"
        2. ssh -i "awsPemKey.pem" ec2-user@ec2-54-210-161-116.compute-1.amazonaws.com

-- Update and setup Instance --

        1. sudo yum update -y
        2. sudo yum install python3-pip -y
        3. pip3 --version
        4. sudo yum install git -y  # For Amazon Linux or RHEL-based EC2 instances
        5. ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
        6. Add the SSH key to GitHub:
                Copy the public key to your clipboard:
                cat ~/.ssh/id_rsa.pub
        7. cd /home/ec2-user/telgram-bot-project  # Replace with your desired directory
        8. git clone https://github.com/yourusername/yourrepository.git
        9. git clone git@github.com:yourusername/yourrepository.git






-- APP RUN --

        1. pip install -r requirements.txt
        2. python3 run.py



